<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/style.css" />
    <title>Vacation Destinations WishList</title>
  </head>
  <body>
    <header>
      <h1>Vacation Destinations WishList</h1>
      <p>
        Found exciting places to visition on your next vacation? Use the form
        below to add them to your wishlist
      </p>
    </header>

    <section>
      <section class="left-half">
        <form
          id="destinationForm"
          name="destinationForm"
          action="/destinations"
          method="post"
        >
          <div class="form-group">
            <label for="DestinationName">Destination name</label>
            <input
              required
              minlength="2"
              type="text"
              class="form-control"
              placeholder="Enter the name of the destination"
              name="name"
            />
          </div>

          <div class="form-group">
            <label for="Location">Location</label>
            <input
              type="text"
              required
              minlength="2"
              class="form-control"
              placeholder="Where is it located?"
              name="location"
            />
          </div>

          <label for="Description">Description</label>
          <textarea
            name="Description"
            class="textArea"
            rows="5"
            placeholder="Where is it located?"
          ></textarea>

          <button type="submit" class="AddToList">Add to list</button>
        </form>
      </section>

      <section class="right-half">
        <h2>Enter destination details</h2>
        <section class="container">
          <section class="row">
            <% for(let i = 0; i < destinations.length; i++){ %>
            <div
              class="card"
              style="width: 15rem"
              data-requestId=<%= destinations[i].id %>
              > <img class="card-img-top" src=<%= destinations[i].photo ===
              undefined ?
              "https://images.unsplash.com/photo-1473442240418-452f03b7ae40?ixid=MnwyNzYyMDZ8MHwxfHNlYXJjaHwxfHxkZXN0aW5hdGlvbnxlbnwwfHx8fDE2Mzg3NDgwMjg&ixlib=rb-1.2.1"
              : destinations[i].photo %> alt="" />
              <div class="card-body">
                <h5 class="card-title"><%= destinations[i].name %></h5>
                <h6 class="card-title"><%= destinations[i].location %></h6>
                <p class="card-text">test</p>
                <div class="buttons">
                  <a class="btn btn-warning" id="Edit">Edit</a>
                  <a class="btn btn-danger" id="Remove">Remove</a>
                </div>
              </div>
            </div>
            <% } %>
          </section>
        </section>
      </section>
    </section>

    <script type="text/javascript" src="main.js"></script>
  </body>
</html>
